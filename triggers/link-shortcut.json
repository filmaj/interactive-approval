{
  "type": "shortcut",
  "name": "Make a Removal",
  "description": "request removal",
  "workflow": "#/workflows/remove_wf",
  "shortcut": {},
  "inputs": {
    "interaction": {
      "value": "{{data.interactivity}}"
    },
    "approval_channel_id": {
      "value": "{{data.channel_id}}"
    }
  }
}
